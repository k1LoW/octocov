## Repositories

| Repository | Coverage | Code to Test Ratio | Time Execution Time | Badges |
| --- | --- | --- | --- | --- |
{{- range $r := .Reports }}
| [{{ $r.Repository }}]({{ $.Host }}/{{ $r.Repository }}) | {{ $r | coverage }} | {{ $r | ratio }} | {{ $r | time }} | {{ badge $r "coverage" $r.Repository (printf "%s/%s/%s/coverage.svg%s" $.RootURL $.BadgesURLRel $r.Repository $.Query) }}{{ if $r.CodeToTestRatio }} {{ badge $r "ratio" $r.Repository (printf "%s/%s/%s/ratio.svg%s" $.RootURL $.BadgesURLRel $r.Repository $.Query) }}{{ end }}{{ if $r.TestExecutionTime }} {{ badge $r "time" $r.Repository (printf "%s/%s/%s/time.svg%s" $.RootURL $.BadgesURLRel $r.Repository $.Query) }}{{ end }} <details><summary>Copy status badge markdown</summary>```{{ badge $r "coverage" "Coverage" (printf "%s/%s/%s/coverage.svg%s" $.RootURL $.BadgesURLRel $r.Repository $.Query) }}```{{ if $r.CodeToTestRatio }}<br>```{{ badge $r "ratio" "Code to Test Ratio" (printf "%s/%s/%s/ratio.svg%s" $.RootURL $.BadgesURLRel $r.Repository $.Query) }}```{{ end }}{{ if $r.TestExecutionTime }}<br>```{{ badge $r "time" "Test Execution Time" (printf "%s/%s/%s/time.svg%s" $.RootURL $.BadgesURLRel $r.Repository $.Query) }}```{{ end }}</details> |
{{- end }}

---

> Generated by [octocov](https://github.com/k1LoW/octocov)
